# Parabank Playwright Automation

E2E test automation project for Parabank using Playwright + TypeScript.  
Target: Docker-based local environment + GitHub Actions CI.

## Tech Stack
- Playwright Test
- TypeScript
- Docker (Parabank container)
- GitHub Actions (CI)

## Quick Start
```bash
npm i
docker compose up -d
npm test